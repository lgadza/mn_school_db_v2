# Test Environment Configuration

# Node environment
NODE_ENV=test

# Server configuration
PORT=4000
HOST=localhost
APP_URL=http://localhost:4000

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mn_school_test
DB_USER=postgres
DB_PASSWORD=postgres
DB_DIALECT=postgres

# Security
JWT_SECRET=test_jwt_secret_key_for_testing_purposes_only
JWT_EXPIRES_IN=3600
REFRESH_TOKEN_SECRET=test_refresh_secret_key_for_testing_purposes_only
REFRESH_TOKEN_EXPIRES_IN=604800
COOKIE_SECRET=test_cookie_secret_for_testing

# Redis (for token blacklisting)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Email
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=test_user
SMTP_PASS=test_pass
EMAIL_FROM=test@example.com
EMAIL_FROM_NAME=Test App

# Logging
LOG_LEVEL=debug

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# File upload
MAX_FILE_SIZE=5242880
